# 更新日志 · love agent

## 0.22.4
- **修复毛玻璃材质「一打字就变深」**：底图（毛玻璃的采样来源）改为按屏幕尺寸固定，不再跟窗口尺寸走
  - 原因：键盘弹出使窗口变矮 → 底图重新裁切缩放 → 输入框与消息卡片的取样内容一起变
  - 影响面：输入框 + 消息卡片（聊天页、设置页都已改）
- **修复启动自动检测更新不认开关**：改为等设置读出来再判断（原来首帧用默认值，关掉也照查）
- **修复更新检查可能读到旧版本号**：请求加时间戳打散中间层缓存

## 0.22.3
- **软件名改为「吃白饭的大肥鱼」**：桌面图标名、界面里显示名字的地方、下载页都同步
- **换新图标**：用自定义图片生成全套启动器图标（各尺寸圆角/圆形 + 自适应图标）
- **App 内检查更新**：设置 → 更新 → 「检查更新」；发现新版本会显示版本号、更新说明，点「前往下载」用浏览器打开下载页
- **启动时自动检测更新**：开关（**默认开**），每次打开 App 自动查一次，有新版本弹提示（前往下载 / 稍后）
- **修复输入框错位边框**：从空到有字时不再出现一条与毛玻璃不重合的边框
- 版本信息读 <https://xingran666.xyz/love-version.json>，失败退回 GitHub

## 0.22.2
- **新消息通知**：App 在后台时收到回答就弹系统通知；点通知回到 App 并切到那个会话；设置里可开关；Android 13+ 首次启动申请通知权限
- **一键免电池优化**：设置 → 通知 → 「免电池优化（后台更稳）」，点一下跳到系统授权页（后台不容易被掐）
- **回到底部修正**：不在底部就显示「↓ 回到底部」；**有未读时变成「↓ N 条未读」**，点击回到**你还没读的第一条**（不是直接跳最底）

## 0.22.1
- **系统提示词修好且有默认「无」**：全局提示词之前根本没下发（设了等于没设），现在按「本会话专属 → 全局 → 都空则不发」；新装默认就是「无」
- **回到底部**：翻走历史后有新消息时，输入栏上方出现「↓ N 条未读」，点一下回到底部
- **分屏关闭修正**：叉掉哪一栏就关哪一栏（之前总是保留主窗）
- 界面材质统一：输入框只在**打字那块圆角**做毛玻璃（底栏不模糊）；指令按钮、指令面板、分屏的连接/模型小标与关闭键都改成**毛玻璃**
- 顶栏与输入栏改为**垂直渐变淡出**（不再是一条硬边）

## 0.22.0
- **每个窗口可单独选连接**：任意 API 直连 或 网关任选（窗口顶部「连接」小标点开切换）
  - 设置里新增「打开新窗口默认用哪台」，新窗口默认跟它；窗口内可选「跟随设置默认」
  - 发送 / `/stop` / 斜杠指令本地拦截判断 / 换模型下发，全部按**该窗口实际连接**走
- 转发到另一栏前先**确认**（「是否操作？」→ 转发 / 取消）
- 设置里可自定义**分屏方向**，改成**一格单选**：左右 / 上下
- **思考面板收起时显示最新思考**（末 3 行），不再只留标题
- 界面：顶栏与输入栏去掉硬边，改为**垂直渐变淡出**（气泡毛玻璃保留）

## 0.21.0
- 分屏双栏：多窗口抽屉里每条会话有「⫽ 分屏」按钮，点一下即与该会话并排显示
  - 两栏各自独立：可同时生成、切换不打断
  - 每栏可点顶部**模型名**单独选模型（互不影响）
  - 消息下「⤴ 转发到另一栏」实现两栏互传
  - 每栏顶部「✕」退出分屏
- 危险命令审批：网关执行需审批的操作时，App 弹「请求操作许可」框 —— **仅单次允许 / 始终允许 / 取消**

## 0.20.0
- 危险命令审批（网关模式）：把网关的审批动作推给 App 弹出，用户选择后回传放行/拒绝

## 0.19.0
- 输入栏左侧新增「/」指令图标：点开按连接方式列出可用指令（网关=全部，API=本地可执行的）
